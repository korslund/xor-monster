These files contain valid Monster tokens but not ordered in any
gramatically meaningful way. They are only meant to test the
tokenizer.

As an interesting note, this README file itself qualifies as a valid
token test.
